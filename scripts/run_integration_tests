#!/usr/bin/env bash
set -euo pipefail

# Default to A09 shield (NT35510 panel). Override via SHIELD if needed.
export SHIELD="${SHIELD:-st_b_lcd40_dsi1_mb1166_a09}"
"$(dirname "$0")"/test_integration.sh "$@"
